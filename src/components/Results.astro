---
import Container from "./Container.astro";
import Topic from "./Topic.astro";

const expectations = [
  {
    label: "Transparent communication",
    detail: "Clear weekly updates on what we worked on, what changed, and what we are testing next.",
  },
  {
    label: "Structured execution",
    detail: "A practical sprint plan across channels, with prioritized tasks instead of random marketing activity.",
  },
  {
    label: "Long-term partnership mindset",
    detail: "We optimize from fundamentals and iterate with you, rather than overselling short-term vanity outcomes.",
  },
];
---

<section id="results" class="pt-8 md:pt-12">
  <Container>
    <div class="section-shell p-5 sm:p-7 md:p-10">
      <Topic
        title="What To Expect Working With Us"
        description="We are a new agency, so we lead with honesty, consistency, and execution quality."
      />

      <div class="grid grid-cols-1 gap-3 md:grid-cols-3">
        {expectations.map((item) => (
          <article class="rounded-2xl border border-[#1f2d4a24] bg-white/85 p-5">
            <h3 class="text-lg font-bold text-[#111c34]">{item.label}</h3>
            <p class="mt-2 text-sm leading-relaxed text-[#4b5c82]">{item.detail}</p>
          </article>
        ))}
      </div>
    </div>
  </Container>
</section>
